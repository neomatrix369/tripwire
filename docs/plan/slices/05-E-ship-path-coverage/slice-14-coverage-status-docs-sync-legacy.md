# Deprecated: canonical source moved

This document is a legacy copy.
Use the canonical slice plan instead:

- `docs/plan/slices/05-E-ship-path-coverage/slice-14-coverage-status-docs-sync.md`

The legacy content was merged into the canonical slice plan; this file is retained only for compatibility and historical traceability.
