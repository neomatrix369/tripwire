// Lab smoke config for softdevteam/snare.
// Secret is the PUBLIC GitHub docs / snare test fixture value "secretsecret"
// (see https://docs.github.com/en/webhooks/using-webhooks/validating-webhook-deliveries
// and snare tests/auth.rs). NOT a production credential — replace for real hooks.

listen = "0.0.0.0:8000";

github {
  match ".*" {
    cmd = "/bin/true";
    secret = "secretsecret";
  }
}
