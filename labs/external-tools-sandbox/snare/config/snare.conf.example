# Example only — copy to snare.conf and set your own secret via local .env /
# untracked override. Never commit real webhook secrets.
#
# listen = "0.0.0.0:8000";
# github {
#   match ".*" {
#     cmd = "/bin/true";
#     secret = "REPLACE_ME";
#   }
# }

listen = "0.0.0.0:8000";

github {
  match ".*" {
    cmd = "/bin/true";
    # Lab placeholder — same public fixture as snare upstream tests.
    secret = "secretsecret";
  }
}
