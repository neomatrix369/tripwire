# Optional tool-local placeholders (prefer ../.env.example).
# Copy values into labs/external-tools-sandbox/.env — never commit secrets.

WTM_BUILD_TARGET=all
# EXT_TOOLS_RUNTIME=docker
# WTM_IMAGE_TAG=who-targets-me:lab

# Facebook / extension login is HITL — leave empty for build-only smoke:
# WTM_FB_EMAIL=
# WTM_FB_PASSWORD=
